#if 1
#include<iostream>
using namespace std;
void logr(const char* message)
{
    cout << message << endl;        //What is output buffer?An output buffer is a location in memory or cache where data ready to be 
                                    //seen is held until the display device is ready. Buffer, Memory terms, Output.//
                                    //What does flush mean in programming?To flush, means to empty the buffer. Now, buffer is temporary 
                                    //storage area for storing data. Both endl and \n can be used to print newline character but there 
                                    //is minor difference between these two : In case of endl, buffer is cleared by usage of internal 
                                    //call to flush the buffer.// 
}
#endif